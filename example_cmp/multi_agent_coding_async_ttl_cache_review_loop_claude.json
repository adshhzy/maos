{
  "id": "multi-agent-coding-async-ttl-cache-review-loop-claude",
  "name": "多 Claude 循环审查编码示例 - AsyncTTLCache 小库实现",
  "graph_type": "control_flow",
  "default_join": "all",
  "max_total_visits": 18,
  "input": {
    "task": "请实现一个可复用的 Python 小库 `maos_cache.py`，提供生产可用的 `AsyncTTLCache` 与同步/异步装饰器能力。必须只使用 Python 标准库。最终交付只需要完整 `maos_cache.py` 主体代码、设计说明和最小可运行使用示例；不要生成 pytest、unittest 或任何测试文件代码。\n\n显式公共 API 契约如下，必须逐项满足：\n1. 模块导入必须成立：`from maos_cache import AsyncTTLCache, CacheInfo, ttl_cache, async_ttl_cache`。\n2. `AsyncTTLCache` 必须提供 `get_or_set`、`aget_or_set`、`invalidate`、`clear`、`cache_info`。\n3. `CacheInfo` 必须是可用于 `isinstance(info, CacheInfo)` 判断的类型；`cache_info()` 必须返回 `CacheInfo` 实例；实例必须至少包含字段/属性：`hits`、`misses`、`evictions`、`inflight`、`size`。字段名必须是 `size`，不要改成 `currsize`、`current_size` 或其他名字。\n4. `get_or_set(key, factory)` 是同步接口：命中缓存返回缓存值；未命中时调用同步 `factory`，并缓存返回值。\n5. `aget_or_set(key, factory)` 是异步接口：未命中时等待异步 `factory`；同一个 key 的并发调用必须 single-flight，只执行一次 factory，其他 awaiter 等待同一个结果。\n6. 统计语义：首个实际执行 factory 的调用计入 `misses`；缓存命中计入 `hits`；并发等待同一个 inflight 结果的 awaiter 计入 `hits`；factory 完成或失败后 `inflight` 必须归零。\n7. TTL 语义：超过 `ttl` 后条目必须过期并重新计算；TTL 过期删除不计入 `evictions`；过期后的重新计算应计入新的 `misses`。\n8. LRU 语义：`maxsize` 达到上限时淘汰最久未使用条目；命中缓存必须刷新最近使用顺序；只有 `maxsize` 触发的 LRU 淘汰才计入 `evictions`；`cache_info().size` 不得超过 `maxsize`。\n9. 异常语义：同步或异步 factory 抛异常时不得缓存异常结果；必须清理 inflight；后续调用同 key 必须可以重新执行 factory。\n10. `invalidate(key)` 和装饰器上的 `wrapper.invalidate(*args, **kwargs)` 必须按与缓存键一致的规则只删除目标条目：删除成功返回 `True`，目标不存在返回 `False`，不得影响其他 key。\n11. `clear()` 和装饰器上的 `wrapper.clear()` 必须清空已缓存条目并重置 `hits`、`misses`、`evictions` 等统计；返回清空前的 `CacheInfo` 快照，不能返回 `None`。\n12. `ttl_cache(maxsize=..., ttl=...)` 必须用于同步函数；`async_ttl_cache(maxsize=..., ttl=...)` 必须用于 async 函数；命名不得替换为 `cached`、`acached`、`async_cache` 等其他名字。\n13. 两个装饰器返回的 wrapper 必须使用 `functools.wraps` 或等效方式保留原函数的 `__name__` 和 `__doc__`，并直接暴露 `.cache_info()`、`.invalidate(...)`、`.clear()` 控制方法。\n14. 装饰器缓存键必须同时考虑 `args` 和 `kwargs`，并且语义相同的关键字参数顺序不能造成不同缓存键，例如 `f(3, scale=2, offset=1)` 与 `f(3, offset=1, scale=2)` 应命中同一条缓存。\n15. 不同 key 的异步调用应能独立执行；同 key 的并发异常应只执行一次失败 factory，并把同一个异常传播给所有等待者。"
  },
  "start": "requirements_architect",
  "nodes": [
    {
      "id": "requirements_architect",
      "label": "需求澄清与接口设计",
      "operation": "agent_task",
      "max_visits": 1,
      "agent": {
        "backend": "claude",
        "agent_key": "architect",
        "context_policy": "provided_context_only",
        "runtime_profile": "claude_print",
        "execution_mode": "claude_print",
        "poll_seconds": 30,
        "timeout_seconds": 1800,
        "artifact_transfer_mode": "inline",
        "result_text_limit": 9000,
        "prompt_payload_limit": 22000,
        "bare": true,
        "prompt": "请实现一个可复用的 Python 小库 `maos_cache.py`，提供生产可用的 `AsyncTTLCache` 与同步/异步装饰器能力。必须只使用 Python 标准库。最终交付只需要完整 `maos_cache.py` 主体代码、设计说明和最小可运行使用示例；不要生成 pytest、unittest 或任何测试文件代码。\n\n显式公共 API 契约如下，必须逐项满足：\n1. 模块导入必须成立：`from maos_cache import AsyncTTLCache, CacheInfo, ttl_cache, async_ttl_cache`。\n2. `AsyncTTLCache` 必须提供 `get_or_set`、`aget_or_set`、`invalidate`、`clear`、`cache_info`。\n3. `CacheInfo` 必须是可用于 `isinstance(info, CacheInfo)` 判断的类型；`cache_info()` 必须返回 `CacheInfo` 实例；实例必须至少包含字段/属性：`hits`、`misses`、`evictions`、`inflight`、`size`。字段名必须是 `size`，不要改成 `currsize`、`current_size` 或其他名字。\n4. `get_or_set(key, factory)` 是同步接口：命中缓存返回缓存值；未命中时调用同步 `factory`，并缓存返回值。\n5. `aget_or_set(key, factory)` 是异步接口：未命中时等待异步 `factory`；同一个 key 的并发调用必须 single-flight，只执行一次 factory，其他 awaiter 等待同一个结果。\n6. 统计语义：首个实际执行 factory 的调用计入 `misses`；缓存命中计入 `hits`；并发等待同一个 inflight 结果的 awaiter 计入 `hits`；factory 完成或失败后 `inflight` 必须归零。\n7. TTL 语义：超过 `ttl` 后条目必须过期并重新计算；TTL 过期删除不计入 `evictions`；过期后的重新计算应计入新的 `misses`。\n8. LRU 语义：`maxsize` 达到上限时淘汰最久未使用条目；命中缓存必须刷新最近使用顺序；只有 `maxsize` 触发的 LRU 淘汰才计入 `evictions`；`cache_info().size` 不得超过 `maxsize`。\n9. 异常语义：同步或异步 factory 抛异常时不得缓存异常结果；必须清理 inflight；后续调用同 key 必须可以重新执行 factory。\n10. `invalidate(key)` 和装饰器上的 `wrapper.invalidate(*args, **kwargs)` 必须按与缓存键一致的规则只删除目标条目：删除成功返回 `True`，目标不存在返回 `False`，不得影响其他 key。\n11. `clear()` 和装饰器上的 `wrapper.clear()` 必须清空已缓存条目并重置 `hits`、`misses`、`evictions` 等统计；返回清空前的 `CacheInfo` 快照，不能返回 `None`。\n12. `ttl_cache(maxsize=..., ttl=...)` 必须用于同步函数；`async_ttl_cache(maxsize=..., ttl=...)` 必须用于 async 函数；命名不得替换为 `cached`、`acached`、`async_cache` 等其他名字。\n13. 两个装饰器返回的 wrapper 必须使用 `functools.wraps` 或等效方式保留原函数的 `__name__` 和 `__doc__`，并直接暴露 `.cache_info()`、`.invalidate(...)`、`.clear()` 控制方法。\n14. 装饰器缓存键必须同时考虑 `args` 和 `kwargs`，并且语义相同的关键字参数顺序不能造成不同缓存键，例如 `f(3, scale=2, offset=1)` 与 `f(3, offset=1, scale=2)` 应命中同一条缓存。\n15. 不同 key 的异步调用应能独立执行；同 key 的并发异常应只执行一次失败 factory，并把同一个异常传播给所有等待者。\n\n你是 Python 库架构师。请把任务拆成清晰的实现规格，输出不可变公共 API 契约、关键行为语义和验收清单。必须明确 `CacheInfo.size`、`invalidate -> bool`、`clear -> CacheInfo`、装饰器控制方法、kwargs 归一化、single-flight、异常不缓存、TTL/LRU 统计语义。不要写完整实现代码。\n\n执行限制：不要调用 Bash、Python、WebFetch、curl、wget 或任何外部工具；不要写入本地文件，不要运行自我验证脚本，不要创建测试文件。上游节点输出已经由持久化执行层注入到当前输入中；如需自检，只能用文字 checklist 逐项核对。",
        "disable_tools": true,
        "resolve_artifacts_before_prompt": true,
        "tool_policy": "none",
        "tools": [],
        "allowed_tools": []
      },
      "timeout_seconds": 2100
    },
    {
      "id": "core_implementation",
      "label": "核心实现编码与修订",
      "operation": "agent_task",
      "join": "any",
      "max_visits": 3,
      "deps": [
        "requirements_architect",
        "quality_review_gate"
      ],
      "agent": {
        "backend": "claude",
        "agent_key": "coding",
        "context_policy": "provided_context_only",
        "runtime_profile": "claude_print",
        "execution_mode": "claude_print",
        "poll_seconds": 30,
        "timeout_seconds": 3000,
        "artifact_transfer_mode": "inline",
        "result_text_limit": 18000,
        "prompt_payload_limit": 50000,
        "bare": true,
        "prompt": "请实现一个可复用的 Python 小库 `maos_cache.py`，提供生产可用的 `AsyncTTLCache` 与同步/异步装饰器能力。必须只使用 Python 标准库。最终交付只需要完整 `maos_cache.py` 主体代码、设计说明和最小可运行使用示例；不要生成 pytest、unittest 或任何测试文件代码。\n\n显式公共 API 契约如下，必须逐项满足：\n1. 模块导入必须成立：`from maos_cache import AsyncTTLCache, CacheInfo, ttl_cache, async_ttl_cache`。\n2. `AsyncTTLCache` 必须提供 `get_or_set`、`aget_or_set`、`invalidate`、`clear`、`cache_info`。\n3. `CacheInfo` 必须是可用于 `isinstance(info, CacheInfo)` 判断的类型；`cache_info()` 必须返回 `CacheInfo` 实例；实例必须至少包含字段/属性：`hits`、`misses`、`evictions`、`inflight`、`size`。字段名必须是 `size`，不要改成 `currsize`、`current_size` 或其他名字。\n4. `get_or_set(key, factory)` 是同步接口：命中缓存返回缓存值；未命中时调用同步 `factory`，并缓存返回值。\n5. `aget_or_set(key, factory)` 是异步接口：未命中时等待异步 `factory`；同一个 key 的并发调用必须 single-flight，只执行一次 factory，其他 awaiter 等待同一个结果。\n6. 统计语义：首个实际执行 factory 的调用计入 `misses`；缓存命中计入 `hits`；并发等待同一个 inflight 结果的 awaiter 计入 `hits`；factory 完成或失败后 `inflight` 必须归零。\n7. TTL 语义：超过 `ttl` 后条目必须过期并重新计算；TTL 过期删除不计入 `evictions`；过期后的重新计算应计入新的 `misses`。\n8. LRU 语义：`maxsize` 达到上限时淘汰最久未使用条目；命中缓存必须刷新最近使用顺序；只有 `maxsize` 触发的 LRU 淘汰才计入 `evictions`；`cache_info().size` 不得超过 `maxsize`。\n9. 异常语义：同步或异步 factory 抛异常时不得缓存异常结果；必须清理 inflight；后续调用同 key 必须可以重新执行 factory。\n10. `invalidate(key)` 和装饰器上的 `wrapper.invalidate(*args, **kwargs)` 必须按与缓存键一致的规则只删除目标条目：删除成功返回 `True`，目标不存在返回 `False`，不得影响其他 key。\n11. `clear()` 和装饰器上的 `wrapper.clear()` 必须清空已缓存条目并重置 `hits`、`misses`、`evictions` 等统计；返回清空前的 `CacheInfo` 快照，不能返回 `None`。\n12. `ttl_cache(maxsize=..., ttl=...)` 必须用于同步函数；`async_ttl_cache(maxsize=..., ttl=...)` 必须用于 async 函数；命名不得替换为 `cached`、`acached`、`async_cache` 等其他名字。\n13. 两个装饰器返回的 wrapper 必须使用 `functools.wraps` 或等效方式保留原函数的 `__name__` 和 `__doc__`，并直接暴露 `.cache_info()`、`.invalidate(...)`、`.clear()` 控制方法。\n14. 装饰器缓存键必须同时考虑 `args` 和 `kwargs`，并且语义相同的关键字参数顺序不能造成不同缓存键，例如 `f(3, scale=2, offset=1)` 与 `f(3, offset=1, scale=2)` 应命中同一条缓存。\n15. 不同 key 的异步调用应能独立执行；同 key 的并发异常应只执行一次失败 factory，并把同一个异常传播给所有等待者。\n\n你是 Python 核心实现工程师。请严格基于 `requirements_architect` 的公共 API 契约实现 `maos_cache.py`，不得改名、合并或替换公共 API。\n\n工作方式：\n1. 首次执行时输出完整 `maos_cache.py` 主体代码，并附最小可运行使用示例；不要生成测试文件代码。\n2. 如果上游包含你上一轮的 `core_implementation`，它是待修订版本，必须以该版本为基础做定向修订，不要从零重写。\n3. 如果上游包含 `quality_review_gate` 的 `decision=needs_revision`，必须逐条吸收 `api_contract_violations`、`implementation_required_changes`、`concurrency_required_changes`、`boundary_required_changes` 和 `acceptance_checks`。\n4. 输出必须包含完整可复制代码块，不能只输出 diff。\n\n提交前必须自检：`CacheInfo` 字段名是 `size`；`clear()` 返回清空前 `CacheInfo`；`invalidate()` 返回 bool；装饰器 wrapper 直接暴露 `.cache_info/.invalidate/.clear`；kwargs 顺序不影响缓存键；LRU 命中刷新 recency；TTL 过期不计 eviction；异步同 key single-flight；factory 异常不缓存且清理 inflight。\n\n执行限制：不要调用 Bash、Python、WebFetch、curl、wget 或任何外部工具；不要写入本地文件，不要运行自我验证脚本，不要创建测试文件。上游节点输出已经由持久化执行层注入到当前输入中；如需自检，只能用文字 checklist 逐项核对。",
        "disable_tools": true,
        "resolve_artifacts_before_prompt": true,
        "tool_policy": "none",
        "tools": [],
        "allowed_tools": []
      },
      "timeout_seconds": 3300
    },
    {
      "id": "test_engineer",
      "label": "测试用例设计与修订",
      "operation": "agent_task",
      "join": "any",
      "max_visits": 3,
      "deps": [
        "requirements_architect",
        "quality_review_gate"
      ],
      "agent": {
        "backend": "claude",
        "agent_key": "qa",
        "context_policy": "provided_context_only",
        "runtime_profile": "claude_print",
        "execution_mode": "claude_print",
        "poll_seconds": 30,
        "timeout_seconds": 2400,
        "artifact_transfer_mode": "inline",
        "result_text_limit": 14000,
        "prompt_payload_limit": 50000,
        "bare": true,
        "prompt": "请实现一个可复用的 Python 小库 `maos_cache.py`，提供生产可用的 `AsyncTTLCache` 与同步/异步装饰器能力。必须只使用 Python 标准库。最终交付只需要完整 `maos_cache.py` 主体代码、设计说明和最小可运行使用示例；不要生成 pytest、unittest 或任何测试文件代码。\n\n显式公共 API 契约如下，必须逐项满足：\n1. 模块导入必须成立：`from maos_cache import AsyncTTLCache, CacheInfo, ttl_cache, async_ttl_cache`。\n2. `AsyncTTLCache` 必须提供 `get_or_set`、`aget_or_set`、`invalidate`、`clear`、`cache_info`。\n3. `CacheInfo` 必须是可用于 `isinstance(info, CacheInfo)` 判断的类型；`cache_info()` 必须返回 `CacheInfo` 实例；实例必须至少包含字段/属性：`hits`、`misses`、`evictions`、`inflight`、`size`。字段名必须是 `size`，不要改成 `currsize`、`current_size` 或其他名字。\n4. `get_or_set(key, factory)` 是同步接口：命中缓存返回缓存值；未命中时调用同步 `factory`，并缓存返回值。\n5. `aget_or_set(key, factory)` 是异步接口：未命中时等待异步 `factory`；同一个 key 的并发调用必须 single-flight，只执行一次 factory，其他 awaiter 等待同一个结果。\n6. 统计语义：首个实际执行 factory 的调用计入 `misses`；缓存命中计入 `hits`；并发等待同一个 inflight 结果的 awaiter 计入 `hits`；factory 完成或失败后 `inflight` 必须归零。\n7. TTL 语义：超过 `ttl` 后条目必须过期并重新计算；TTL 过期删除不计入 `evictions`；过期后的重新计算应计入新的 `misses`。\n8. LRU 语义：`maxsize` 达到上限时淘汰最久未使用条目；命中缓存必须刷新最近使用顺序；只有 `maxsize` 触发的 LRU 淘汰才计入 `evictions`；`cache_info().size` 不得超过 `maxsize`。\n9. 异常语义：同步或异步 factory 抛异常时不得缓存异常结果；必须清理 inflight；后续调用同 key 必须可以重新执行 factory。\n10. `invalidate(key)` 和装饰器上的 `wrapper.invalidate(*args, **kwargs)` 必须按与缓存键一致的规则只删除目标条目：删除成功返回 `True`，目标不存在返回 `False`，不得影响其他 key。\n11. `clear()` 和装饰器上的 `wrapper.clear()` 必须清空已缓存条目并重置 `hits`、`misses`、`evictions` 等统计；返回清空前的 `CacheInfo` 快照，不能返回 `None`。\n12. `ttl_cache(maxsize=..., ttl=...)` 必须用于同步函数；`async_ttl_cache(maxsize=..., ttl=...)` 必须用于 async 函数；命名不得替换为 `cached`、`acached`、`async_cache` 等其他名字。\n13. 两个装饰器返回的 wrapper 必须使用 `functools.wraps` 或等效方式保留原函数的 `__name__` 和 `__doc__`，并直接暴露 `.cache_info()`、`.invalidate(...)`、`.clear()` 控制方法。\n14. 装饰器缓存键必须同时考虑 `args` 和 `kwargs`，并且语义相同的关键字参数顺序不能造成不同缓存键，例如 `f(3, scale=2, offset=1)` 与 `f(3, offset=1, scale=2)` 应命中同一条缓存。\n15. 不同 key 的异步调用应能独立执行；同 key 的并发异常应只执行一次失败 factory，并把同一个异常传播给所有等待者。\n\n你是 Python 测试工程师。本图保留测试设计节点，用于帮助实现节点发现边界问题，但最终交付仍不得包含测试文件。请输出一份测试设计说明和伪代码级验收清单，不要输出完整 pytest 文件。必须覆盖：`CacheInfo.size`、`invalidate -> bool`、`clear -> CacheInfo`、wrapper 控制方法、kwargs 归一化、TTL/LRU、同 key async single-flight、不同 key 独立执行、异常不缓存和 inflight 清理。\n\n执行限制：不要调用 Bash、Python、WebFetch、curl、wget 或任何外部工具；不要写入本地文件，不要运行自我验证脚本，不要创建测试文件。上游节点输出已经由持久化执行层注入到当前输入中；如需自检，只能用文字 checklist 逐项核对。",
        "disable_tools": true,
        "resolve_artifacts_before_prompt": true,
        "tool_policy": "none",
        "tools": [],
        "allowed_tools": []
      },
      "timeout_seconds": 2700
    },
    {
      "id": "quality_review_gate",
      "label": "质量审查门",
      "operation": "agent_task",
      "join": "all",
      "max_visits": 3,
      "deps": [
        "core_implementation",
        "test_engineer"
      ],
      "agent": {
        "backend": "claude",
        "agent_key": "reviewer",
        "context_policy": "provided_context_only",
        "runtime_profile": "claude_print",
        "execution_mode": "claude_print",
        "poll_seconds": 30,
        "timeout_seconds": 2400,
        "artifact_transfer_mode": "inline",
        "result_text_limit": 12000,
        "prompt_payload_limit": 70000,
        "strict_structured_output": true,
        "risk_pass_requires_final_visit": true,
        "required_structured_output_fields": [
          "decision",
          "reason",
          "required_changes",
          "implementation_required_changes",
          "test_required_changes",
          "failing_or_missing_test_scenarios",
          "implementation_feedback_from_tests",
          "acceptance_checks",
          "risks"
        ],
        "bare": true,
        "prompt": "你是严格的 Python 实现正确性审查门。审查 `core_implementation` 的最终实现，并参考 `test_engineer` 的测试设计说明，判断是否满足上游公共 API 契约和原始任务。请先读取节点上下文中的 `control_flow.visit` 作为当前审查轮次，不能臆测轮次。\n\n审查必须像隐藏验收一样具体，逐项验证：\n1. `from maos_cache import AsyncTTLCache, CacheInfo, ttl_cache, async_ttl_cache` 能成立，公共符号没有改名。\n2. `AsyncTTLCache` 有 `get_or_set`、`aget_or_set`、`invalidate`、`clear`、`cache_info`。\n3. `cache_info()` 返回 `CacheInfo` 实例；`CacheInfo` 必须有 `hits/misses/evictions/inflight/size`，字段名必须是 `size`，出现 `currsize/current_size` 但没有 `size` 必须判为 API 契约违背。\n4. `get_or_set` 同步缓存命中/未命中语义正确。\n5. `aget_or_set` 对同 key 并发 single-flight，只执行一次 factory；其他 awaiter 共享结果且计入 hits；不同 key 可独立执行。\n6. factory 抛异常不得缓存；同 key 并发异常只执行一次失败 factory；异常传播给等待者；inflight 必须清理，后续可重试。\n7. TTL 过期会重新计算，且 TTL 过期不计入 evictions。\n8. maxsize LRU 淘汰最久未使用条目；命中必须刷新 recency；`cache_info().size <= maxsize`。\n9. `invalidate(key)` 和 wrapper `.invalidate(*args, **kwargs)` 必须只删除目标 key，命中返回 `True`，不存在返回 `False`。\n10. `clear()` 和 wrapper `.clear()` 必须返回清空前 `CacheInfo` 快照，并在清空后重置缓存和统计，不能返回 `None`。\n11. `ttl_cache` / `async_ttl_cache` wrapper 必须保留 `__name__`、`__doc__`，并直接暴露 `.cache_info/.invalidate/.clear`。\n12. 装饰器缓存键必须统一 args/kwargs，关键字参数顺序不同但语义相同应命中同一缓存。\n13. 只使用标准库，输出代码块完整可复制。\n\n最终只能输出一个合法 JSON 对象，不能输出 markdown、解释、思考过程或额外文字。格式严格如下：{\"decision\":\"approved、needs_revision 或 approved_with_risk\",\"reason\":\"一句中文审查结论\",\"required_changes\":[\"总体必须修改事项；通过时为空数组\"],\"api_contract_violations\":[\"公共 API 契约违背项；没有则为空数组\"],\"implementation_required_changes\":[\"一般实现修订项；没有则为空数组\"],\"concurrency_required_changes\":[\"并发/single-flight/异常传播修订项；没有则为空数组\"],\"boundary_required_changes\":[\"TTL/LRU/边界/统计语义修订项；没有则为空数组\"],\"acceptance_checks\":[\"下一轮必须满足的验收断言；通过时也要保留关键断言\"],\"risks\":[\"带风险通过时必须列出遗留风险；其他情况可为空数组\"],\"confidence\":0.0}。\n\n当 `control_flow.visit` 小于 3 时：禁止输出 approved_with_risk；只要任一问题字段非空，就必须输出 needs_revision。只有实现完整满足 API、并发、边界和统计语义且问题字段均为空，才允许输出 approved。当 `control_flow.visit` 等于 3 时：不允许输出 needs_revision；如果仍有非阻塞遗留问题，输出 approved_with_risk，并把遗留问题放入 risks、required_changes 和对应分类字段；如果没有遗留问题，输出 approved。\n\n执行限制：不要调用 Bash、Python、WebFetch、curl、wget 或任何外部工具；不要写入本地文件，不要运行自我验证脚本，不要创建测试文件。上游节点输出已经由持久化执行层注入到当前输入中；如需自检，只能用文字 checklist 逐项核对。",
        "disable_tools": true,
        "resolve_artifacts_before_prompt": true,
        "tool_policy": "none",
        "tools": [],
        "allowed_tools": []
      },
      "timeout_seconds": 2700
    },
    {
      "id": "final_integrator",
      "label": "最终整合交付",
      "operation": "agent_task",
      "join": "any",
      "deps": [
        "requirements_architect",
        "core_implementation",
        "test_engineer",
        "quality_review_gate"
      ],
      "agent": {
        "backend": "claude",
        "agent_key": "coordinator",
        "context_policy": "provided_context_only",
        "runtime_profile": "claude_print",
        "execution_mode": "claude_print",
        "poll_seconds": 30,
        "timeout_seconds": 3600,
        "artifact_transfer_mode": "inline",
        "result_text_limit": 28000,
        "prompt_payload_limit": 90000,
        "bare": true,
        "prompt": "请实现一个可复用的 Python 小库 `maos_cache.py`，提供生产可用的 `AsyncTTLCache` 与同步/异步装饰器能力。必须只使用 Python 标准库。最终交付只需要完整 `maos_cache.py` 主体代码、设计说明和最小可运行使用示例；不要生成 pytest、unittest 或任何测试文件代码。\n\n显式公共 API 契约如下，必须逐项满足：\n1. 模块导入必须成立：`from maos_cache import AsyncTTLCache, CacheInfo, ttl_cache, async_ttl_cache`。\n2. `AsyncTTLCache` 必须提供 `get_or_set`、`aget_or_set`、`invalidate`、`clear`、`cache_info`。\n3. `CacheInfo` 必须是可用于 `isinstance(info, CacheInfo)` 判断的类型；`cache_info()` 必须返回 `CacheInfo` 实例；实例必须至少包含字段/属性：`hits`、`misses`、`evictions`、`inflight`、`size`。字段名必须是 `size`，不要改成 `currsize`、`current_size` 或其他名字。\n4. `get_or_set(key, factory)` 是同步接口：命中缓存返回缓存值；未命中时调用同步 `factory`，并缓存返回值。\n5. `aget_or_set(key, factory)` 是异步接口：未命中时等待异步 `factory`；同一个 key 的并发调用必须 single-flight，只执行一次 factory，其他 awaiter 等待同一个结果。\n6. 统计语义：首个实际执行 factory 的调用计入 `misses`；缓存命中计入 `hits`；并发等待同一个 inflight 结果的 awaiter 计入 `hits`；factory 完成或失败后 `inflight` 必须归零。\n7. TTL 语义：超过 `ttl` 后条目必须过期并重新计算；TTL 过期删除不计入 `evictions`；过期后的重新计算应计入新的 `misses`。\n8. LRU 语义：`maxsize` 达到上限时淘汰最久未使用条目；命中缓存必须刷新最近使用顺序；只有 `maxsize` 触发的 LRU 淘汰才计入 `evictions`；`cache_info().size` 不得超过 `maxsize`。\n9. 异常语义：同步或异步 factory 抛异常时不得缓存异常结果；必须清理 inflight；后续调用同 key 必须可以重新执行 factory。\n10. `invalidate(key)` 和装饰器上的 `wrapper.invalidate(*args, **kwargs)` 必须按与缓存键一致的规则只删除目标条目：删除成功返回 `True`，目标不存在返回 `False`，不得影响其他 key。\n11. `clear()` 和装饰器上的 `wrapper.clear()` 必须清空已缓存条目并重置 `hits`、`misses`、`evictions` 等统计；返回清空前的 `CacheInfo` 快照，不能返回 `None`。\n12. `ttl_cache(maxsize=..., ttl=...)` 必须用于同步函数；`async_ttl_cache(maxsize=..., ttl=...)` 必须用于 async 函数；命名不得替换为 `cached`、`acached`、`async_cache` 等其他名字。\n13. 两个装饰器返回的 wrapper 必须使用 `functools.wraps` 或等效方式保留原函数的 `__name__` 和 `__doc__`，并直接暴露 `.cache_info()`、`.invalidate(...)`、`.clear()` 控制方法。\n14. 装饰器缓存键必须同时考虑 `args` 和 `kwargs`，并且语义相同的关键字参数顺序不能造成不同缓存键，例如 `f(3, scale=2, offset=1)` 与 `f(3, offset=1, scale=2)` 应命中同一条缓存。\n15. 不同 key 的异步调用应能独立执行；同 key 的并发异常应只执行一次失败 factory，并把同一个异常传播给所有等待者。\n\n你是最终代码交付工程师。只有在 `quality_review_gate` 的 `decision=approved` 或 `decision=approved_with_risk` 后才执行。\n\n关键原则：你的职责是保真封装上游最终代码，不是重新设计、重新生成或摘要代码。`core_implementation` 才是代码所有权节点；你必须以它的最新完整 `maos_cache.py` 代码块作为最终交付代码。\n\n输出必须严格按以下顺序：\n1. `## 完整 maos_cache.py`\n2. 一个且仅一个完整 Python 代码块。这个代码块必须逐字复制最新一轮 `core_implementation` 输出中的完整 `maos_cache.py` 代码块，不得重写、压缩、省略、合并、重新组织或根据自己的理解重新实现。\n3. 公共 API 契约对照表，逐项说明 `AsyncTTLCache`、`CacheInfo.size`、`ttl_cache`、`async_ttl_cache`、`get_or_set`、`aget_or_set`、`invalidate -> bool`、`clear -> CacheInfo`、`cache_info` 是否满足。\n4. 并发、TTL/LRU、异常不缓存、统计语义的设计说明。\n5. 最小可运行使用示例。\n6. 如果审查门是 `approved_with_risk`，单独列出遗留风险、影响范围和后续修复建议。\n\n交付前必须做保真自检：第一段 `maos_cache.py` 代码块里必须真实出现以下顶层定义，而不是只出现在 `__all__`、说明文字或清单里：\n- `class AsyncTTLCache`\n- `class CacheInfo` 或等价的 `CacheInfo = ...` 类型定义\n- `def ttl_cache`\n- `def async_ttl_cache`\n\n如果任一顶层定义缺失，说明你没有正确复制上游核心实现，必须回到 `core_implementation` 的最新完整代码块重新复制，不能交付缺失公共 API 的版本。\n\n不得把 `size` 改成 `currsize`；不得删除 wrapper 的 `.cache_info/.invalidate/.clear`；不得把 `clear()` 改成返回 `None`；不得生成 pytest、unittest 或任何测试文件代码。\n\n执行限制：不要调用 Bash、Python、WebFetch、curl、wget 或任何外部工具；不要写入本地文件，不要运行自我验证脚本，不要创建测试文件。上游节点输出已经由持久化执行层注入到当前输入中；如需自检，只能用文字 checklist 逐项核对。",
        "disable_tools": true,
        "resolve_artifacts_before_prompt": true,
        "tool_policy": "none",
        "tools": [],
        "allowed_tools": []
      },
      "timeout_seconds": 3900,
      "params": {
        "prompt": "请实现一个可复用的 Python 小库 `maos_cache.py`，提供生产可用的 `AsyncTTLCache` 与同步/异步装饰器能力。必须只使用 Python 标准库。最终交付只需要完整 `maos_cache.py` 主体代码、设计说明和最小可运行使用示例；不要生成 pytest、unittest 或任何测试文件代码。\n\n显式公共 API 契约如下，必须逐项满足：\n1. 模块导入必须成立：`from maos_cache import AsyncTTLCache, CacheInfo, ttl_cache, async_ttl_cache`。\n2. `AsyncTTLCache` 必须提供 `get_or_set`、`aget_or_set`、`invalidate`、`clear`、`cache_info`。\n3. `CacheInfo` 必须是可用于 `isinstance(info, CacheInfo)` 判断的类型；`cache_info()` 必须返回 `CacheInfo` 实例；实例必须至少包含字段/属性：`hits`、`misses`、`evictions`、`inflight`、`size`。字段名必须是 `size`，不要改成 `currsize`、`current_size` 或其他名字。\n4. `get_or_set(key, factory)` 是同步接口：命中缓存返回缓存值；未命中时调用同步 `factory`，并缓存返回值。\n5. `aget_or_set(key, factory)` 是异步接口：未命中时等待异步 `factory`；同一个 key 的并发调用必须 single-flight，只执行一次 factory，其他 awaiter 等待同一个结果。\n6. 统计语义：首个实际执行 factory 的调用计入 `misses`；缓存命中计入 `hits`；并发等待同一个 inflight 结果的 awaiter 计入 `hits`；factory 完成或失败后 `inflight` 必须归零。\n7. TTL 语义：超过 `ttl` 后条目必须过期并重新计算；TTL 过期删除不计入 `evictions`；过期后的重新计算应计入新的 `misses`。\n8. LRU 语义：`maxsize` 达到上限时淘汰最久未使用条目；命中缓存必须刷新最近使用顺序；只有 `maxsize` 触发的 LRU 淘汰才计入 `evictions`；`cache_info().size` 不得超过 `maxsize`。\n9. 异常语义：同步或异步 factory 抛异常时不得缓存异常结果；必须清理 inflight；后续调用同 key 必须可以重新执行 factory。\n10. `invalidate(key)` 和装饰器上的 `wrapper.invalidate(*args, **kwargs)` 必须按与缓存键一致的规则只删除目标条目：删除成功返回 `True`，目标不存在返回 `False`，不得影响其他 key。\n11. `clear()` 和装饰器上的 `wrapper.clear()` 必须清空已缓存条目并重置 `hits`、`misses`、`evictions` 等统计；返回清空前的 `CacheInfo` 快照，不能返回 `None`。\n12. `ttl_cache(maxsize=..., ttl=...)` 必须用于同步函数；`async_ttl_cache(maxsize=..., ttl=...)` 必须用于 async 函数；命名不得替换为 `cached`、`acached`、`async_cache` 等其他名字。\n13. 两个装饰器返回的 wrapper 必须使用 `functools.wraps` 或等效方式保留原函数的 `__name__` 和 `__doc__`，并直接暴露 `.cache_info()`、`.invalidate(...)`、`.clear()` 控制方法。\n14. 装饰器缓存键必须同时考虑 `args` 和 `kwargs`，并且语义相同的关键字参数顺序不能造成不同缓存键，例如 `f(3, scale=2, offset=1)` 与 `f(3, offset=1, scale=2)` 应命中同一条缓存。\n15. 不同 key 的异步调用应能独立执行；同 key 的并发异常应只执行一次失败 factory，并把同一个异常传播给所有等待者。\n\n你是最终代码交付工程师。只有在 `quality_review_gate` 的 `decision=approved` 或 `decision=approved_with_risk` 后才执行。\n\n关键原则：你的职责是保真封装上游最终代码，不是重新设计、重新生成或摘要代码。`core_implementation` 才是代码所有权节点；你必须以它的最新完整 `maos_cache.py` 代码块作为最终交付代码。\n\n输出必须严格按以下顺序：\n1. `## 完整 maos_cache.py`\n2. 一个且仅一个完整 Python 代码块。这个代码块必须逐字复制最新一轮 `core_implementation` 输出中的完整 `maos_cache.py` 代码块，不得重写、压缩、省略、合并、重新组织或根据自己的理解重新实现。\n3. 公共 API 契约对照表，逐项说明 `AsyncTTLCache`、`CacheInfo.size`、`ttl_cache`、`async_ttl_cache`、`get_or_set`、`aget_or_set`、`invalidate -> bool`、`clear -> CacheInfo`、`cache_info` 是否满足。\n4. 并发、TTL/LRU、异常不缓存、统计语义的设计说明。\n5. 最小可运行使用示例。\n6. 如果审查门是 `approved_with_risk`，单独列出遗留风险、影响范围和后续修复建议。\n\n交付前必须做保真自检：第一段 `maos_cache.py` 代码块里必须真实出现以下顶层定义，而不是只出现在 `__all__`、说明文字或清单里：\n- `class AsyncTTLCache`\n- `class CacheInfo` 或等价的 `CacheInfo = ...` 类型定义\n- `def ttl_cache`\n- `def async_ttl_cache`\n\n如果任一顶层定义缺失，说明你没有正确复制上游核心实现，必须回到 `core_implementation` 的最新完整代码块重新复制，不能交付缺失公共 API 的版本。\n\n不得把 `size` 改成 `currsize`；不得删除 wrapper 的 `.cache_info/.invalidate/.clear`；不得把 `clear()` 改成返回 `None`；不得生成 pytest、unittest 或任何测试文件代码。\n\n执行限制：不要调用 Bash、Python、WebFetch、curl、wget 或任何外部工具；不要写入本地文件，不要运行自我验证脚本，不要创建测试文件。上游节点输出已经由持久化执行层注入到当前输入中；如需自检，只能用文字 checklist 逐项核对。"
      }
    }
  ],
  "edges": [
    {
      "from": "requirements_architect",
      "to": "core_implementation",
      "label": "规格驱动实现"
    },
    {
      "from": "requirements_architect",
      "to": "test_engineer",
      "label": "规格驱动测试"
    },
    {
      "from": "core_implementation",
      "to": "quality_review_gate",
      "label": "提交实现审查"
    },
    {
      "from": "test_engineer",
      "to": "quality_review_gate",
      "label": "提交测试审查"
    },
    {
      "from": "quality_review_gate",
      "to": "core_implementation",
      "when": "(last.decision == 'needs_revision' or last.decision == 'approved_with_risk') and visits.quality_review_gate < 3",
      "kind": "loop",
      "label": "前两轮审查未通过：回退实现修订"
    },
    {
      "from": "quality_review_gate",
      "to": "test_engineer",
      "when": "(last.decision == 'needs_revision' or last.decision == 'approved_with_risk') and visits.quality_review_gate < 3",
      "kind": "loop",
      "label": "前两轮审查未通过：回退测试修订"
    },
    {
      "from": "quality_review_gate",
      "to": "final_integrator",
      "when": "last.decision == 'approved'",
      "kind": "branch",
      "label": "审查通过：最终整合"
    },
    {
      "from": "quality_review_gate",
      "to": "final_integrator",
      "when": "last.decision == 'approved_with_risk' and visits.quality_review_gate >= 3",
      "kind": "branch",
      "label": "最后一轮带风险通过：最终整合"
    }
  ],
  "description": "多 Agent 循环审查编码图：架构、实现、测试设计、质量审查和最终整合协作；所有确定性评测中的公共 API 与行为要求均已显式写入任务。",
  "metadata": {
    "comparison_group": "coding_async_ttl_cache",
    "comparison_role": "multi_agent_review_loop",
    "backend_variant": "claude",
    "loop_policy": {
      "review_node": "quality_review_gate",
      "pass_decision": "approved",
      "risk_pass_decision": "approved_with_risk",
      "retry_decision": "needs_revision",
      "max_review_visits": 3,
      "max_revision_visits": 3,
      "retry_allowed_until_review_visit": 2
    },
    "expected_advantage": [
      "用审查门显式阻止低质量实现直接进入最终交付",
      "审查门把反馈拆成 implementation_required_changes 与 test_required_changes，避免实现和测试拿到模糊意见",
      "核心实现节点会读取 failing_or_missing_test_scenarios 与 implementation_feedback_from_tests，让测试节点暴露的边界场景反向改进实现",
      "Web UI 可观察每轮审查 decision、reason、分流修订项和 acceptance_checks",
      "最终交付只使用最后一轮通过审查或带风险通过审查的实现与测试"
    ]
  }
}
